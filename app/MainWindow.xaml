<Window x:Class="Timma.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Timma"
        xmlns:cefSharp="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d"
        Title="Timma" Height="720" Width="1280"
        Closing="MainWindow_Closing"
        Background="#1AB394">
    <Window.InputBindings>
        <KeyBinding Key="r" Modifiers="Ctrl" Command="Refresh" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="TimmaErrorBoxWrapper" VerticalAlignment="Top" Background="#FFCF3333" Grid.Row="0" Height="30" Visibility="Collapsed">
            <TextBlock x:Name="TimmaErrorBox" HorizontalAlignment="Center"  Foreground="White" FontFamily="Helvetica" Cursor="Arrow" FontSize="16" IsEnabled="True" Padding="0,5">
            </TextBlock>
        </Grid>

        <Grid x:Name="TimmaBrowser" Grid.Row="1">
        </Grid>
    </Grid>
</Window>
